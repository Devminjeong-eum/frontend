const QUERY_KEYS = Object.freeze({
  HOME_KEY: 'posts',
  QUIZ_KEY: 'quiz',
  SEARCH_KEY: 'search',
  LIKEDWORD_KEY: 'likedWord',
  AUTH_KEY: 'auth',
  USER_KEY: 'user',
  TREND: 'trend',
  TTS_KEY: 'ttsUrl',
} as const);

export default QUERY_KEYS;
